# ============================================================
# necoocean-vault — 文档仓库 .gitignore
#
# 依据：docs/文档规范/仓库形态规范.md v1.2
#   约束 R1  工作区根不得初始化 git（本仓库根 = 工作区根，故须排除兄弟仓库）
#   约束 R3  文档仓库根须有 .gitignore 排除两个代码仓库（与采用布局 A/B 无关）
# ============================================================

# --- 同级代码仓库（各自是独立 git 仓库，不得纳入本仓库）---
necoocean-tools-backend/
necoocean-tools-frontend/

# --- 工作区私有数据（不进版本控制）---
.workbuddy/

# --- 临时产物 ---
.tmp-research/

# --- 操作系统与编辑器噪声 ---
Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
*.swp
*~

# --- 递归兜底：任何层级的 .git 目录均不得被跟踪 ---
.git/
