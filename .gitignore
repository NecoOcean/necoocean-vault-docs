# ============================================================
# necoocean-vault-docs -- 文档仓库 .gitignore
#
# 依据：文档规范/仓库形态规范.md v1.2
#   约束 R3  文档仓库根须有 .gitignore 排除两个代码仓库
#
# ! 本文件所在目录即仓库根：docs/
#   故两个兄弟仓库位于「上一级」，必须用 ../ 前缀
# ============================================================

# --- 兄弟代码仓库（各自是独立 git 仓库，不得纳入本仓库）---
#     物理位置：necoocean-vault\ 下，与本仓库根 docs\ 同级
#     即本仓库根的上一级
../necoocean-tools-backend/
../necoocean-tools-frontend/

# --- 工作区私有数据（位于上一级，不进版本控制）---
../.workbuddy/
../.tmp-research/

# --- 操作系统与编辑器噪声 ---
Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
*.swp
*~

# --- 递归兜底：任何层级的 .git 目录均不得被跟踪 ---
.git/
