# 变更记录

本文件记录文档仓库的重要变更，并维护三仓库的版本对应表。格式遵循 Keep a Changelog。尚未对外发布的内容留在 `[Unreleased]`。各文档自己的修订记录仍写在该文档末尾，不在这里逐条重复。

## [Unreleased]

### Added

- 后端已完成开发顺序第 1～9 步：至对象存储与数据导出（C-42 + `export_audit_logs`）
- 前端仓库 `necoocean-tools-frontend`：Vue 3 + Vite，公开页（首页 / 专题页 / 关于我）+ 后台管理（登录 / 仪表盘 / 工具 / 分类 / 留言 / 设置 / 导出）全套功能面
- 部署运维手册：`docs/doc/03-进度与交付/部署运维手册.md`（v1.1）

### Changed

- 进度更新至 2026-09-22 晚：前端一期功能面已齐并完成视觉对齐；**四条真实链路（下载 302 / 留言提交 / 管理员回复 / 更新日志与导出）已全部在真实数据上跑通**，取代此前"导出链路尚未真机冒烟"的记录
- UI 视觉决策 **D-1 / D-2 / D-3 / D-5 全部闭环**（D-8 随 D-7 延后）：`--tx-3` 提升至 ≥4.5:1，主按钮默认态改用 `--brand-dim` 以修复白字对比度；`UI设计规范` 升 **v1.2**，§十一 由「待确认项」改为「决策台账」
- D-7 定为一期不启用全站背景图，首页保持纯色底
- 部署手册含 COS 环境变量；对象存储已接入、下载可 302

### Fixed

- **本文件自身结论更正**：原记录"导出链路尚未真机冒烟（`export_audit_logs` 仍 0 行）"**已被实测推翻**——该表现有 **1 行**（`include_email=0`，证明 ADR-002「默认不含邮箱」在真实链路上生效）
- `docs/doc/03-进度与交付/部署运维手册.md` **v1.0 → v1.1**：环境文件模板由「缺 `COS_*`」改为**补入五项**（原模板照抄会使对象存储降级为 `Unavailable`，上传与签名下载全部不可用）；前端仓库状态由"还没有建立"改为**已建立**并删去过渡说明；迁移版本号由 `V3` / `4` 统一更正为 **`V5` / `5`**；§2.8 补入导出与审计、`cover_url` 两条核验项
- `docs/doc/02-设计与实现/验收用例集.md` **v1.0 → v1.1**：**B-30（封面图 2 MB 上限）标注为一期延后**——封面上传未实现（错误码 `42208` 无调用点、无端点、前端无入口），该用例一期不可执行；一期可验收条数由 34 条改为 **33 条**；关联需求版本由 PRD v1.7 校正为 **v1.8**
- `README.md` 项目状态表更正：UI 层未决项由「**5 项**」改为**已全部闭环或延后**；"前端仓库尚未建立"改为**已建立**；文档导航补「怎么部署上线」一行
- `docs/doc/03-进度与交付/项目进度与下一步分析.md` 最近核对更新至 **2026-09-23**；§一 补「一期可验收 33 条」与「可部署、不可公开」的上线判定；新增 §1.1「一期唯一的验收缺口」（B-30）
- `docs/doc/01-需求与范围/设计决策记录-托管范围与留言联系方式.md` §3 补时效标注：文中"前端仓库尚未建立"为 **2026-09-21 决策时状态**（三仓库已于 2026-09-22 全部建成并推送），避免被读作现状

### Security

- 数据库口令、管理员口令、IP 哈希盐与 COS 密钥不进仓库。本机放在已忽略的 `config/local.yml`
- 导出默认不含邮箱；显式 `include_email=true` 才输出，并写审计记录
- 前端 CSRF 与后端一致：`csrf_token` Cookie + `X-CSRF-Token` 头

## [0.1.0] - 2026-09-21

### Added

- PRD、一期清单、设计决策记录、系统设计、架构说明、附录 C、技术选型、参数台账、验收用例集
- UI 设计规范 v1.1 与首页原型
- 文档规范与仓库形态约定

### Fixed

- 系统设计中的外键数由 4 条更正为 5 条

## 版本对应表

尚未打发布标签。下表只记录当前对照，正式发布时再补 tag。

| 发布日期 | 文档版本 | 后端 | 前端 |
|---|---|---|---|
| 未发布 | PRD v1.8 · 附录 C v0.8 · 系统设计 v1.0 · 验收用例集 v1.1 · UI设计规范 v1.2 | `main` @ `979af4a` | `main` @ `338c982` |

> 上表 sha 于 **2026-09-23 09:30** 以 `git ls-remote` 复核，与两仓库本地 `HEAD` 逐字相等。前端本轮较上一版前进 1 个提交（`5daee37` → `338c982`）。
