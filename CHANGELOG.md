# 变更记录

本文件记录文档仓库的重要变更，并维护三仓库的版本对应表。格式遵循 Keep a Changelog。尚未对外发布的内容留在 `[Unreleased]`。各文档自己的修订记录仍写在该文档末尾，不在这里逐条重复。

## [Unreleased]

### Added

- 后端已完成开发顺序第 1～9 步：至对象存储与数据导出（C-42 + `export_audit_logs`）
- 前端仓库 `necoocean-tools-frontend`：Vue 3 + Vite，公开页（首页 / 专题页 / 关于我）+ 后台管理（登录 / 仪表盘 / 工具 / 分类 / 留言 / 设置 / 导出）全套功能面
- 部署运维手册：`docs/doc/03-进度与交付/部署运维手册.md`（v1.1）
- **备案期间部署执行单**：`docs/doc/03-进度与交付/备案期间部署执行单.md`（v1.0）——按「ICP 备案未通过」约束把手册 §二 重排为八阶段可打勾路径，含 VS Code Remote-SSH 连接方式、端口转发本地浏览器联调、§2.8 清单按可验性分流

### Changed

- 进度更新至 2026-09-22 晚：前端一期功能面已齐并完成视觉对齐；**四条真实链路（下载 302 / 留言提交 / 管理员回复 / 更新日志与导出）已全部在真实数据上跑通**，取代此前"导出链路尚未真机冒烟"的记录
- UI 视觉决策 **D-1 / D-2 / D-3 / D-5 全部闭环**（D-8 随 D-7 延后）：`--tx-3` 提升至 ≥4.5:1，主按钮默认态改用 `--brand-dim` 以修复白字对比度；`UI设计规范` 升 **v1.2**，§十一 由「待确认项」改为「决策台账」
- D-7 定为一期不启用全站背景图，首页保持纯色底
- 部署手册含 COS 环境变量；对象存储已接入、下载可 302

### Fixed

- **本文件自身结论更正**：原记录"导出链路尚未真机冒烟（`export_audit_logs` 仍 0 行）"**已被实测推翻**——该表现有 **1 行**（`include_email=0`，证明 ADR-002「默认不含邮箱」在真实链路上生效）
- `docs/doc/03-进度与交付/部署运维手册.md` **v1.0 → v1.1**：环境文件模板由「缺 `COS_*`」改为**补入五项**（原模板照抄会使对象存储降级为 `Unavailable`，上传与签名下载全部不可用）；前端仓库状态由"还没有建立"改为**已建立**并删去过渡说明；迁移版本号由 `V3` / `4` 统一更正为 **`V5` / `5`**；§2.8 补入导出与审计、`cover_url` 两条核验项；§一 补「**操作系统必须是纯净的系统镜像**」并写明判据只看控制台「镜像名称 / 镜像类型 / 操作系统」三行、**不得用「实例名称」判断**；§2.6 补 IPv6 监听注意事项；§2.7 补顺序约束——**HTTPS 证书必须等 ICP 备案通过之后再签**（`certbot --nginx` 走 HTTP-01 验证，需 80 端口对外可达，备案前必失败）
- `docs/doc/02-设计与实现/验收用例集.md` **v1.0 → v1.1**：**B-30（封面图 2 MB 上限）标注为一期延后**——封面上传未实现（错误码 `42208` 无调用点、无端点、前端无入口），该用例一期不可执行；一期可验收条数由 34 条改为 **33 条**；关联需求版本由 PRD v1.7 校正为 **v1.8**
- **Nginx `http2` 写法口径冲突已修**：`部署运维手册` §2.6 的 443 段原写 `listen 443 ssl;` + `http2 on;`，而 `http2 on;` 是独立指令、**自 nginx 1.25.1 才引入**；2026-09-23 在项目服务器实测 nginx 为 **1.18.0**，该写法会让 `nginx -t` 报 `unknown directive "http2"`。已改为 **`listen 443 ssl http2;`**，与 `前后端分离架构说明` §七 的既有写法对齐；`备案期间部署执行单` 同步升 **v1.1** 并在 Phase 十 加该约束
- **修两处 2026-09-23 首次真实部署中踩到的陷阱**：① MySQL 建库账号**必须同时建 `'necoocean'@'localhost'` 与 `'necoocean'@'127.0.0.1'`**——Ubuntu 的 MySQL 8 `skip_name_resolve` 默认为 **0**，TCP 来自 127.0.0.1 会被反解成 `localhost`，只建 IP 那条会导致应用启动报 `Access denied ... 'necoocean'@'localhost'`（Error Code 1045），**口令正确也被拒**；② 环境文件改写为「**变量 + 不带引号的 heredoc**」并加 `grep -c '<'` 自检，杜绝「带占位符的 heredoc 被整体照抄」——当日 `DB_PASSWORD=<数据库口令>` 原样入库导致过启动失败。`部署运维手册` §2.3 / §2.4 与 `备案期间部署执行单` v1.4 均已跟进
- **后端新增「启动时自动写入 COS 桶 CORS」能力（`43781f2`），部署文档同步**：新增环境变量 **`COS_CORS_ORIGINS`**（默认值**只含本机开发端口**，须在部署时补生产域名）与 **`COS_CORS_AUTO_APPLY`**（默认 `true`）；写入语义是 `setBucketCrossOriginConfiguration` 的**整体替换**而非追加，故不可与控制台两处同时维护；CAM 子用户需具备 **`PutBucketCORS`**；写入失败只记 error 日志、**不阻塞启动**。`部署运维手册` §2.4 / §3.1 / §3.3 与 `备案期间部署执行单` v1.3 均已跟进（环境变量 12 → **14 项**，备案期该值填端口转发的本地地址、备案后须改为真实域名）
- **两处既定决策未落到部署手册，已补**：① systemd 单元缺 **`-Xmx1g`**（《技术选型与工程结构（方案对比）》规定 4 GB 机器堆上限 1G、切勿超 2G）；② 缺 **`innodb_buffer_pool_size = 1G`** 的设置步骤。二者原先只存在于技术选型文档，导致实际运行参数与文档决策对不上账。现已写入 `部署运维手册` §2.3 / §2.4，并以**独立配置文件 `zz-necoocean.cnf`** 承载缓冲池设置（不改发行版自带 `mysqld.cnf`）；`备案期间部署执行单` 同步升 **v1.2**
- `README.md` 项目状态表更正：UI 层未决项由「**5 项**」改为**已全部闭环或延后**；"前端仓库尚未建立"改为**已建立**；文档导航补「怎么部署上线」一行
- `docs/doc/03-进度与交付/项目进度与下一步分析.md` 最近核对更新至 **2026-09-23**；§一 补「一期可验收 33 条」与「可部署、不可公开」的上线判定；新增 §1.1「一期唯一的验收缺口」（B-30）
- `docs/doc/01-需求与范围/设计决策记录-托管范围与留言联系方式.md` §3 补时效标注：文中"前端仓库尚未建立"为 **2026-09-21 决策时状态**（三仓库已于 2026-09-22 全部建成并推送），避免被读作现状

### Security

- 数据库口令、管理员口令、IP 哈希盐与 COS 密钥不进仓库。本机放在已忽略的 `config/local.yml`
- 导出默认不含邮箱；显式 `include_email=true` 才输出，并写审计记录
- 前端 CSRF 与后端一致：`csrf_token` Cookie + `X-CSRF-Token` 头

## [0.1.0] - 2026-09-21

### Added

- PRD、一期清单、设计决策记录、系统设计、架构说明、附录 C、技术选型、参数台账、验收用例集
- UI 设计规范 v1.1 与首页原型
- 文档规范与仓库形态约定

### Fixed

- 系统设计中的外键数由 4 条更正为 5 条

## 版本对应表

尚未打发布标签。下表只记录当前对照，正式发布时再补 tag。

| 发布日期 | 文档版本 | 后端 | 前端 |
|---|---|---|---|
| 未发布 | PRD v1.8 · 附录 C v0.8 · 系统设计 v1.0 · 验收用例集 v1.1 · UI设计规范 v1.2 | `main` @ `43781f2` | `main` @ `338c982` |

> 上表 sha 于 **2026-09-23 11:35** 以 `git ls-remote` 复核，与两仓库本地 `HEAD` 逐字相等。后端本轮前进 1 个提交：`979af4a` → **`43781f2`**（启动时自动写入 COS 桶 CORS）。
