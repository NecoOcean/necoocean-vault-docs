# 项目进度与下一步分析

- **生成日期**：2026-09-21；**最近核对**：2026-09-22
- **分析基线**：本地代码、`mvn -B verify` 与两个 Git 仓库，非文档自述
- **文档性质**：进度快照。2026-09-21 那一版已过时，本文按 2026-09-22 重测替换结论

---

## 一、结论先行

| 阶段 | 状态 |
|---|---|
| 需求与设计文档层 | ✅ **完成**，无未决参数 |
| 文档规范体系 | ✅ **完成**，11 类文档规范 + 图表/绘图两规范 |
| 后端 | 🟡 **9/10 步**。第 9 步导出（C-42 + 审计）已落地，`mvn -B verify` 通过（84 个测试，1 个跳过）。余第 10 步前端 |
| 远端代码 | ✅ 本提交后与本地 `main` 对齐 |
| 本机库 | ✅ Flyway 需执行到 **V5**（`export_audit_logs`）。V4 已跑过；启动一次即可升到 V5 |
| 前端 | ⬜ **未开始**（仓库尚未建立）。首页可以按 D-7 开工 |
| UI 设计 | 🟡 规范 v1.1 + 首页原型完成。D-7 已定为一期不启用背景图。仍待确认 D-1、D-2、D-3、D-5、D-8 |
| 部署运维手册（N-2） | ✅ 已建 |
| CHANGELOG（N-3） | ✅ 已建 |

**下一步**：前端仓库开工（开发顺序第 10 步）。后端 JSON API 一期能力已齐。

---

## 二、已完成的工作

### 2.1 仓库结构（实测）

```
necoocean-vault/                    ← 本地工作区，本身不是 git 根
├── docs/                           ← 文档仓库（远端 necoocean-vault-docs，已有 git）
│   ├── doc/                        # 四分类（2026-09-21 重组）
│   │   ├── 01-需求与范围/          3 份
│   │   ├── 02-设计与实现/          6 份
│   │   ├── 03-进度与交付/          2 份
│   │   └── _archive/              3 份（快照，不作事实源）
│   ├── design/
│   │   ├── UI设计规范.md
│   │   └── prototype/index.html    首页高保真原型
│   └── 文档规范/                   8 份（撰写与图表唯一判据来源）
├── necoocean-tools-backend/        ← 独立 git 仓库，已关联 GitHub remote
└── .workbuddy/memory/              工作日志 + 长期备忘
```

### 2.4 后端（2026-09-22 实测）

| 项 | 证据 |
|---|---|
| 项目坐标 | `com.necoocean.tools`，`0.1.0-SNAPSHOT`，父 POM Spring Boot **3.5.16**，Java **17** |
| 第 1～8 步 | 日志、契约、数据层、认证、公开读、留言、后台管理、COS 两阶段直传与下载 302 |
| 第 9 步·导出 | C-42：`GET /api/v1/admin/export`；`type`×`format`；`include_email` 默认 false；含邮箱时表头警示；写 `export_audit_logs`（Flyway V5） |
| 质量门禁 | Checkstyle + p3c-pmd + JaCoCo ≥70%。**84 个测试，1 个跳过** |
| 真实 COS | 本机已冒烟：C-28→PUT→C-29→302 |

**尚未引入**：Lombok、任何模板引擎（Enforcer 已禁止模板引擎）。

---

## 三、当前进展定位

按《系统设计文档》§八 的 10 步开发顺序：

| 序 | 模块 | 状态 |
|---|---|---|
| 1～8 | 日志～对象存储 | ✅ 完成并推送 |
| 9 | 导出（C-42 + 审计） | ✅ 完成。默认不含邮箱；显式 `include_email=true` 才输出并写审计 |
| 10 | 前端（Vue 3 + Vite） | ⬜ **下一步** |

**完成度粗估**：后端 API 约 **90%**（9/10 步）；整体项目（含前端）约 **50%**。

---

## 四、下一步待办（按优先级）

### P0 — 主线

#### 1. 前端首页

D-7 已定为一期不启用全站背景图。公开读、留言、上传凭证、下载 302、导出接口本地均已就绪。

#### 2. 本机升到 Flyway V5

实体已含 `ExportAuditLog`。启动一次应用即可；**库落后代码时 `ddl-auto=validate` 会启动失败**。

### P1 — 可并行

| 项 | 说明 |
|---|---|
| 留言 / 更新日志真实数据冒烟 | `messages` / `release_notes` 业务链路单元测试已绿，真实库行仍可补冒烟 |
| 上线前再抽测真实 COS | 自动化仍以 memory 为主 |

### P2 — 上线前

CDN、ICP 备案、COS 流量告警、正式域名 HTTPS。

---

## 五、需要注意的风险与遗留

| # | 风险 | 证据 | 影响 |
|---|---|---|---|
| 1 | 自动化未覆盖真实 COS | JaCoCo 排除 `TencentCosObjectStore`；Web 测试用 memory | 低——本机已冒烟；上线前再抽测 |
| 2 | 3 Mbps 带宽 | D-7 一期不用背景图 | 低 |
| 3 | 中文 Windows 读 UTF-8 配置 | `Get-Content` 默认 GBK 可能吞键名 | 低——用 Python UTF-8 读 `local.yml` |

---

## 六、建议的行动顺序

```
1. 本机启动一次应用，确认 Flyway V5
2. 前端仓库开工（首页按纯色底）
3. 可选：留言 / 更新日志真实数据冒烟
```

**一句话**：后端一期 API（至导出）已齐；下一步是前端。
