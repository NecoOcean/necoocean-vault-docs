# 项目进度与下一步分析

- **生成日期**：2026-09-21；**最近核对**：2026-09-22
- **分析基线**：本地代码、`mvn -B verify` 与两个 Git 仓库，非文档自述
- **文档性质**：进度快照。2026-09-21 那一版已过时，本文按 2026-09-22 重测替换结论

---

## 一、结论先行

| 阶段 | 状态 |
|---|---|
| 需求与设计文档层 | ✅ **完成**，无未决参数 |
| 文档规范体系 | ✅ **完成**，11 类文档规范 + 图表/绘图两规范 |
| 后端 | 🟡 **本地 6/10 步**。日志、契约、数据层、认证会话、公开读、留言提交已通过 `mvn -B verify`（72 个测试，1 个因无 Docker 跳过） |
| 远端代码 | 🟡 [necoocean-tools-backend](https://github.com/NecoOcean/necoocean-tools-backend) 的远端仍停在认证会话（`f5164b2`）。公开读和留言提交只在本地 `main` |
| 本机库 | ✅ MySQL 8.4.4 上 Flyway V1～V3 均为 success，`last_failed_at` 已存在 |
| 前端 | ⬜ **未开始**（仓库尚未建立）。首页可以按 D-7 开工 |
| UI 设计 | 🟡 规范 v1.1 + 首页原型完成。D-7 已定为一期不启用背景图。仍待确认 D-1、D-2、D-3、D-5、D-8 |
| 部署运维手册（N-2） | ✅ 已建 |
| CHANGELOG（N-3） | ✅ 已建 |

**下一步**：后台管理 C-13～C-44。对象存储仍是下载签名的前提。

---

## 二、已完成的工作

### 2.1 仓库结构（实测）

```
necoocean-vault/                    ← 本地工作区，本身不是 git 根
├── docs/                           ← 文档仓库（远端 necoocean-vault-docs，已有 git）
│   ├── doc/                        # 四分类（2026-09-21 重组）
│   │   ├── 01-需求与范围/          3 份
│   │   ├── 02-设计与实现/          6 份
│   │   ├── 03-进度与交付/          1 份
│   │   └── _archive/              3 份（快照，不作事实源）
│   ├── design/
│   │   ├── UI设计规范.md
│   │   └── prototype/index.html    首页高保真原型
│   └── 文档规范/                   8 份（撰写与图表唯一判据来源）
├── necoocean-tools-backend/        ← 独立 git 仓库，已关联 GitHub remote
└── .workbuddy/memory/              工作日志 + 长期备忘
```

**各分类收录**：

| 分类 | 收录 |
|---|---|
| `01-需求与范围/` | PRD v1.8 · 一期清单 v1.8 · 设计决策记录（ADR-001/002/003） |
| `02-设计与实现/` | 系统设计文档 v1.0 · 前后端分离架构说明 v1.2 · 附录C v0.8 · 技术选型 v0.5 · 待确认参数台账 v1.0 · 验收用例集 v1.0 |
| `03-进度与交付/` | 项目进度与下一步分析 |
| `_archive/` | 文档图表合规性检查报告 · 需求文档审阅报告（含第二轮） |

**引用口径（2026-09-21 归一，见 `docs/文档规范/README.md` §4.1.2）**：同目录引用写裸文件名；跨目录引用写仓库根相对完整路径（如 `docs/doc/02-设计与实现/系统设计文档.md`）。终验结果：带路径引用 **124 处**，其中悬空 **10 处全部为待建文档**（N-1~N-10）；围栏配对 0 异常。

### 2.2 文档层（已完成）

**事实源分工已确立**：需求→PRD；架构→《前后端分离架构说明》；接口→《附录C》；字段→PRD §8；参数取值→《待确认参数台账》；验收判定→《验收用例集》；视觉→《UI设计规范》；撰写与图表→`docs/文档规范/`。

**已定稿的关键边界**：

1. 「托管」= 仅文件存储与下载（ADR-001）
2. 留言邮箱选填、仅管理员可见，公开接口须序列化白名单（ADR-002）
3. 单文件 200 MB；类型白名单 19 类
4. 审核一期先发后审 + 关键词标记；`audit_status` 字段与接口**一期即建**
5. 首页承载全量工具，一期不单设列表页
6. 备案归属广西管局；一期暂不公开上线

**最紧物理约束**：3 Mbps 带宽（≈375 KB/s）→ 首屏 gzip 后 ≤163 KB → 达标场景**并发峰值 ≤3 人**。

### 2.3 文档规范体系（已完成）

**8 个文件**：`README.md`（总索引 + 文档类型全谱系 + 12 项缺失文档类型 N-1~N-12）+ 通用规范（主干）+ 3 份分类规范（需求类 / 设计与接口类 / 质量与交付类）+ **仓库形态规范**（跨项目强制级）+ 图表绘制规范 + 绘图 prompt。

提炼出既有文档的**五条撰写基因**：结论先行 / 事实源唯一 / 口径显式化 / 不确定性标注 / 变更可追溯。

### 2.4 后端（2026-09-22 实测）

| 项 | 证据 |
|---|---|
| 项目坐标 | `com.necoocean.tools`，`0.1.0-SNAPSHOT`，父 POM Spring Boot **3.5.16**，Java **17** |
| 第 1 步·日志骨架 | `RequestLogFilter`、`LogMasker`、`ApplicationStartupLogger`；访问日志不记查询串和请求体 |
| 第 2 步·统一契约 | `ApiResponse`、`ErrorCode`（30 个业务码 + `40400`）、`BizException`、`GlobalExceptionHandler`、分页 |
| 第 3 步·数据层 | 8 个实体、Flyway `V1`～`V3`、预置 4 个分类和站点配置。库需事先用 `create_database.sql` 建好。方言按 MySQL 8.4 生成 `DATETIME` / `TEXT` / `TINYINT` |
| 第 4 步·认证会话 | Cookie `SESSION`（HttpOnly、Secure、SameSite=Lax，登录起 12 小时绝对过期）；CSRF Cookie `csrf_token`、请求头 `X-CSRF-Token`；连续失败 5 次锁定 15 分钟（账号级，`last_failed_at`）。登录 `POST /api/v1/admin/login` 已用 Apifox 调通 |
| 第 5 步·公开读 | `PublicReadController`：C-01～C-04、C-06、C-08。留言与文件走白名单，不返回 `contact_email`、`ip_hash`、`object_key`。封面地址在对象存储接入前为 `null` |
| 下载 | `GET /download/{file_id}`：不存在或工具已下架为 `40404`；记录存在但还不能签名为 `40405` |
| 管理员 | 库中没有账号且设置了 `ADMIN_PASSWORD` 时创建一次。口令和数据库口令在已忽略的 `config/local.yml`，不进仓库 |
| 质量门禁 | Checkstyle 10.21.4 + 阿里 p3c-pmd 2.1.1 + JaCoCo 行覆盖 ≥70%。2026-09-22：`mvn -B verify` 通过，**62 个测试，1 个跳过** |
| 版本控制 | 远端 `f5164b2` 只含第 1～4 步。公开读仍是本地未提交改动 |

**尚未引入**：COS SDK、留言提交与限流、后台管理、导出、Lombok、任何模板引擎（Enforcer 已禁止模板引擎）。

### 2.5 UI 设计（已完成部分）

- `UI设计规范.md` v1.1：深色默认主题、CSS 变量体系、开场动画（≈2.8s 由真实加载进度驱动）、全站背景图三层结构 + 88% 遮罩（实测 `--tx-2` = 5.90:1）
- `docs/design/prototype/index.html`：首页高保真可交互原型
- 肖像图规格已定：880×1100 / 4:5 / ≤120 KB

---

## 三、当前进展定位

按《系统设计文档》§八 的 10 步开发顺序：

| 序 | 模块 | 状态 |
|---|---|---|
| 1 | 日志骨架 | ✅ 完成 |
| 2 | 统一契约 | ✅ 完成 |
| 3 | 数据层（8 实体 + Flyway + `audit_status` 索引） | ✅ 完成。本机 MySQL 8.4 已执行到 `V3`（含 `last_failed_at`） |
| 4 | 认证会话（Security + CSRF + 登录锁定） | ✅ 完成，已推送 |
| 5 | 公开读接口（C-01~C-06、C-08） | 🟡 本地完成。C-05 只做存在性和上架校验，签名跳转留到第 8 步 |
| 6 | 留言提交（C-07 + C-04b + 限流 + 关键词标记） | ✅ 完成。公网 IP 单工具 3 条/60 秒，同一 IP 5 条/60 秒；内网地址不进限流桶 |
| 7 | 后台管理（C-13~C-44） | ⬜ 下一步 |
| 8 | 文件两阶段直传（C-28~C-33） | ⬜ **风险最高**。做完后 C-05 才能返回 302 |
| 9 | 导出（C-42 + 审计） | ⬜ |
| 10 | 前端（Vue 3 + Vite） | ⬜ |

**可并行分界**：后端 1~3 串行前置已经走完；4~7 与 8~9 可并行；前端自第 5 步完成即可接入。

**完成度粗估**：后端约 **60%**（6/10 步，下载签名仍未做）；整体项目（含前端）约 **35%**。

---

## 四、下一步待办（按优先级）

### P0 — 主线

#### 1. 后端第 7 步：后台管理

留言已经能提交和查询状态。下一步是工具、分类、留言审核和站点设置（C-13～C-44）。

#### 2. 前端首页

D-7 已定为一期不启用全站背景图。第 5 步公开读已经提交到本地 `main`，前端仓库可以按这个口径开工。

### P1 — 可与主线并行

#### 3. 推送本地后端

公开读和留言提交都还没推到 GitHub。推送前确认 `config/local.yml` 仍被忽略。

### P2 — 可并行 / 视情况推进

| 项 | 说明 |
|---|---|
| 关键词表 | 配置键 `message_keywords`，缺省不标记。后台编辑留到第 7 步 |
| 后端第 8~9 步 | 文件两阶段直传（**风险最高，需真实 COS 环境验证**）+ 导出 |
| 30 个业务错误码 | 已落地。新增接口不得再发明第 31 个；未匹配路径继续用 `40400` |
| N-6 数据字典 | 数据层已落地，字典仍缺，用来对齐状态字段口径 |
| 技术选型 §三/§五 作废内容迁 `docs/doc/_archive/` | 规范 §4.3 已提示，非紧急 |

### P3 — 上线前才需要

| 项 | 触发时机 |
|---|---|
| 接入 CDN | 正式公开前，不改业务代码 |
| ICP 备案 | 公开前启动（周期以周计），归属广西管局 |
| 启用 COS 用量告警 | 上线前 |
| 配置正式域名与 HTTPS | 上线前 |

---

## 五、需要注意的风险与遗留

### 5.1 实质风险

| # | 风险 | 证据 | 影响 |
|---|---|---|---|
| 1 | 第 5、6 步尚未推送 | 远端后端停在 `f5164b2` | 中——换机器克隆不到公开读和留言提交 |
| 2 | 文件两阶段直传（第 8 步） | 《系统设计文档》标注"风险最高" | 高——需真实 COS 环境验证，孤儿对象清理须可靠 |
| 3 | 3 Mbps 带宽 | D-7 已定为一期不用背景图 | 低——首页按纯色底实现即可 |
| 4 | 下载链接还不能用 | 已上架文件的 `GET /download/{file_id}` 固定返回 `40405` | 中——对象存储接入前，公开页不能把这条地址当成可下载 |

### 5.2 需保持的口径纪律（易写错）

- 实体关系 **8 实体 / 5 条外键**（正文曾误记 4 条）
- 会话/登录态是**跨前后端两层的共享存储**，不属任何一层独占
- 浅色主题遮罩必须用 `rgba(255,255,255,.88)`，套深色遮罩会让正文对比度跌到 1 附近
- 参数变更须同步检查《待确认参数台账》§四「上游影响索引」列出的全部章节

### 5.3 本机环境（影响执行方式）

- Bash 工具不可用 → 一律用 PowerShell + Read/Write/Edit/Glob/Grep
- PowerShell stdout 常不回传 → 脚本内 `Out-File` 写文件后再 Read
- PowerShell 重定向 `>` 写出 UTF-16LE，Read 会拒读，需先转 UTF-8
- `Add-Type` 被安全策略拦截 → 需 .NET 能力时改用 Python
- 同一文件的多次编辑**不可并行**（后写覆盖前写，且两者都报成功）

---

## 六、建议的行动顺序

```
1. 后端第 7 步：后台管理
2. 推送本地 main
3. 前端首页（不启用全站背景图）
4. 后端第 8～9 步：对象存储与导出
```

**一句话**：留言可以提交；下载链接仍要等对象存储；前端首页不再等背景图决策。
